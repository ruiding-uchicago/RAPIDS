# Core dependencies for RAPIDS (already in README)
fairchem-core>=1.0.0
pubchempy>=1.0.4

# Web GUI dependencies (NEW in v1.2.0)
flask>=2.3.0
flask-cors>=4.0.0

# MCP Server dependencies (NEW in v1.5.0)
mcp>=1.0.0
# --- Core scientific stack (used throughout) ---
numpy
scipy
matplotlib
rdkit
torch
ase
huggingface_hub

# --- Optional: xTB implicit-solvation post-correction ---
xtb

# --- Optional: DFT escalation arms (SP / GeoSP), GPU ---
pyscf
gpu4pyscf
